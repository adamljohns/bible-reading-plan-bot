# Python deps for the auxiliary maintenance scripts in scripts/ and bin/
# (page/reading/dictionary builders, enrichment, audits). The core directory
# product is Node + static HTML in docs/ and needs none of these to deploy.
# Unpinned by preference: install latest. Pin here only if an upgrade breaks a script.
Pillow      # PIL: shield/icon and image generation
requests    # HTTP for enrichment / link-audit scripts
urllib3     # used directly by some audit/fetch scripts
openai      # used by the AI-assisted reading/closer generators
